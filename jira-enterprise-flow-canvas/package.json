{
  "name": "jira-enterprise-flow-canvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"Custom UI is plain static assets for MVP scaffold\"",
    "test": "node --test tests/*.test.js",
    "deploy:prod": "forge deploy -e production && forge install -e production --site=$FORGE_SITE"
  },
  "dependencies": {
    "@forge/api": "latest",
    "@forge/bridge": "latest",
    "@forge/react": "latest",
    "@forge/resolver": "latest",
    "react": "^18.2.0"
  }
}
