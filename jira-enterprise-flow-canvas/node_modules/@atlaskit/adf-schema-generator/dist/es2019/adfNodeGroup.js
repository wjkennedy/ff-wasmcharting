export function adfNodeGroup(group,
// eslint-disable-next-line @typescript-eslint/no-explicit-any
members = [], spec) {
  if (group.includes(' ')) {
    throw new Error('Group name cannot contain spaces');
  }
  for (const member of members) {
    member.setGroup(group);
  }
  return {
    group,
    groupType: 'node',
    members,
    isIgnored: transformerName => {
      var _spec$ignore;
      return (spec === null || spec === void 0 ? void 0 : (_spec$ignore = spec.ignore) === null || _spec$ignore === void 0 ? void 0 : _spec$ignore.includes(transformerName)) || false;
    }
  };
}