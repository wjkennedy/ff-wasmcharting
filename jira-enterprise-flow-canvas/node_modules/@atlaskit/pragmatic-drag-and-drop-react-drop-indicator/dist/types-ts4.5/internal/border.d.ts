/**
 * @jsxRuntime classic
 * @jsx jsx
 */
import type { CSSColor, CSSSize } from '../internal-types';
export declare function Border({ strokeColor, strokeWidth, borderRadius, // TODO: update to radius.small (4px) token
indent, }: {
    strokeColor?: CSSColor;
    borderRadius?: CSSSize;
    strokeWidth?: CSSSize;
    indent?: string;
}): JSX.Element;
export default Border;
