"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.parseUserColor = parseUserColor;
var _hex = require("./formats/hex");
var _hsl = require("./formats/hsl");
var _rgb = require("./formats/rgb");
/**
 * Attempts to parse a CSS color string into a standard RGB format.
 *
 * Supported formats:
 *
 * - Hex
 * - RGB
 * - HSL
 *
 * Returns `null` on failure.
 */
function parseUserColor(color) {
  if (color.startsWith('#')) {
    return (0, _hex.parseHex)(color);
  }
  if (color.startsWith('rgb(')) {
    return (0, _rgb.parseRgb)(color);
  }
  if (color.startsWith('hsl(')) {
    return (0, _hsl.parseHsl)(color);
  }
  return null;
}