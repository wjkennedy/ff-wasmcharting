import React, { forwardRef } from 'react';
import AddIcon from '@atlaskit/icon/core/add';
import { Button } from './themed/migration';
/**
 * __Create button__
 *
 * The create button for the top navigation.
 */
export const CreateButton = /*#__PURE__*/forwardRef(({
  children,
  onClick,
  testId,
  interactionName
}, ref) => /*#__PURE__*/React.createElement(Button, {
  ref: ref,
  appearance: "primary",
  iconBefore: AddIcon,
  onClick: onClick,
  testId: testId,
  interactionName: interactionName
}, children));