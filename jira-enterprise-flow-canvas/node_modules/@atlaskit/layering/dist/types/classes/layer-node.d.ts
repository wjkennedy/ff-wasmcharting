export declare class LayerNode {
    id: string;
    childNodes: LayerNode[];
    parent: LayerNode | null;
    constructor(id: string, parent: LayerNode | null);
    addChild(node: LayerNode): void;
    removeChild(node: LayerNode): void;
    getLevel(): number;
    getHeight(): number;
}
