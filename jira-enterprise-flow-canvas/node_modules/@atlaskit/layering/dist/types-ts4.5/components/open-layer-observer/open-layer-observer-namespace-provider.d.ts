import React from 'react';
/**
 * Creates a layer namespace for grouping layers together.
 */
export declare function OpenLayerObserverNamespaceProvider({ children, namespace, }: {
    children: React.ReactNode;
    namespace: string;
}): React.JSX.Element;
