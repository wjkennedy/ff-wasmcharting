import type { Position } from 'css-box-model';
type Args = {
    scrollHeight: number;
    scrollWidth: number;
    height: number;
    width: number;
};
declare const _default: ({ scrollHeight, scrollWidth, height, width }: Args) => Position;
export default _default;
