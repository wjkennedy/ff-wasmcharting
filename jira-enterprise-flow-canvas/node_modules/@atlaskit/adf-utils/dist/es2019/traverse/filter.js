import { traverse } from '../traverse/traverse';
export function filter(adf, callback) {
  const result = [];
  traverse(adf, {
    any: node => {
      if (callback(node)) {
        result.push(node);
      }
    }
  });
  return result;
}