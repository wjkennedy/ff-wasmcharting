import { applyMark } from '../utils/apply-mark';
export var backgroundColor = function backgroundColor(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'backgroundColor',
      attrs: attrs
    }, maybeNode);
  };
};