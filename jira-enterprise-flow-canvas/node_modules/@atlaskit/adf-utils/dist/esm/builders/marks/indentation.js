import { applyMark } from '../utils/apply-mark';
export var indentation = function indentation(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'indentation',
      attrs: attrs
    }, maybeNode);
  };
};