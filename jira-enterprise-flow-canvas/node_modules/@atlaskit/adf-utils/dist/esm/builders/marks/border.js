import { applyMark } from '../utils/apply-mark';
export var border = function border(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'border',
      attrs: attrs
    }, maybeNode);
  };
};