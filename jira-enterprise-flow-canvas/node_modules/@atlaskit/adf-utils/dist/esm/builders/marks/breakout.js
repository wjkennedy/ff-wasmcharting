import { applyMark } from '../utils/apply-mark';
export var breakout = function breakout(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'breakout',
      attrs: attrs
    }, maybeNode);
  };
};