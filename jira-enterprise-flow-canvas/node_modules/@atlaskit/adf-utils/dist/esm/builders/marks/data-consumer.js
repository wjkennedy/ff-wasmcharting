import { applyMark } from '../utils/apply-mark';
export var dataConsumer = function dataConsumer(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'dataConsumer',
      attrs: attrs
    }, maybeNode);
  };
};