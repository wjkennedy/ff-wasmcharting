import { applyMark } from '../utils/apply-mark';
export var subsup = function subsup(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'subsup',
      attrs: attrs
    }, maybeNode);
  };
};