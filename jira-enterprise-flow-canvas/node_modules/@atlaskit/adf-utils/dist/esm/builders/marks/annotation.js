import { applyMark } from '../utils/apply-mark';
export var annotation = function annotation(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'annotation',
      attrs: attrs
    }, maybeNode);
  };
};