import { applyMark } from '../utils/apply-mark';
export var textColor = function textColor(attrs) {
  return function (maybeNode) {
    return applyMark({
      type: 'textColor',
      attrs: attrs
    }, maybeNode);
  };
};