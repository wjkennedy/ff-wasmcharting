import type { DragController } from '../types';
/**
 * Sets up listeners for pointer dragging.
 */
export declare function usePointerControls({ dragController, contextId, }: {
    dragController: DragController;
    contextId: string;
}): void;
