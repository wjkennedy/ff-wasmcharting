import React from 'react';
import WarningIcon from '@atlaskit/icon/core/status-warning';
import Inline from '@atlaskit/primitives/inline';
import { Y500 } from '@atlaskit/theme/colors';
/**
 * __Footer items__
 *
 * The footer items in the comment. Usually reserved for error messages.
 *
 * @internal
 */
const Footer = ({
  actions = [],
  errorActions = [],
  errorIconLabel,
  isError,
  isSaving,
  testId
}) => {
  if (isSaving || !(actions.length || errorActions.length)) {
    return null;
  }
  const items = isError ? errorActions : actions;
  return /*#__PURE__*/React.createElement(Inline, {
    alignBlock: "center",
    shouldWrap: true,
    testId: testId,
    space: "space.100",
    separator: "\xB7"
  }, isError && /*#__PURE__*/React.createElement(WarningIcon, {
    spacing: "spacious",
    color: `var(--ds-icon-warning, ${Y500})`,
    label: errorIconLabel ? errorIconLabel : ''
  }), items.map((item, key) => Object.assign({}, item, {
    key
  })));
};
Footer.displayName = 'CommentFooter';
export default Footer;