import React from 'react';
export type Locale = {
    value: string;
    label: string;
};
export type LocaleSelectProps = {
    id?: string;
    locales?: Locale[];
    locale?: Locale;
    defaultLocale?: Locale;
    onLocaleChange?: (locale: Locale) => void;
};
export declare const defaultLocales: Locale[];
declare const LocaleSelect: ({ id, locales, locale, defaultLocale, onLocaleChange, }: LocaleSelectProps) => React.JSX.Element;
export default LocaleSelect;
