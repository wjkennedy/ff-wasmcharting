import { ManifestObject, ManifestValidationResult } from '../types';
import { ValidatorInterface } from './validator-interface';
export declare class SchemaValidator<T> implements ValidatorInterface<ManifestObject<T> | undefined, T> {
    private readonly schema;
    private readonly validateSchema;
    constructor(schema: object);
    private processErrorMessage;
    private handleModuleError;
    private handleGenericError;
    private isDeprecatedField;
    private getDeprecationInfo;
    private handleDeprecatedField;
    private handleError;
    validate(manifest: ManifestObject<T> | undefined): Promise<ManifestValidationResult<T>>;
}
//# sourceMappingURL=schema-validator.d.ts.map