"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findPosition = void 0;
const defaultPosition = {
    line: 1,
    column: 0
};
const locateTargetInLines = (target, lines) => {
    let lineNumber = -1;
    for (let index = 0; index < lines.length; index++) {
        const found = lines[index].indexOf(target);
        if (found !== -1) {
            lineNumber = index + 1;
            break;
        }
    }
    return lineNumber;
};
const findPosition = (substring, contentByLine, section) => {
    let lineNumber = 1;
    let columnNumber = 0;
    let sectionLineNumber = 0;
    if (!contentByLine) {
        return defaultPosition;
    }
    if (section) {
        sectionLineNumber = locateTargetInLines(section, contentByLine);
        if (sectionLineNumber == -1) {
            return defaultPosition;
        }
    }
    for (let index = sectionLineNumber; index < contentByLine.length; index++) {
        const found = contentByLine[index].indexOf(substring);
        if (found != -1) {
            lineNumber = index + 1;
            columnNumber = found;
            break;
        }
    }
    return {
        line: lineNumber,
        column: columnNumber
    };
};
exports.findPosition = findPosition;
